# 智餐外卖红包模块：项目讲解与面试准备

> 当前仓库只有优惠券骨架，红包模块仍是设计稿。未实现和未测试前，不应表述为“已经上线”。

## 1. 项目简介（实现后可用）

为智餐外卖设计付费红包资产模块，覆盖套餐购买、支付回调幂等发放、个人资产有效期、订单自动抵扣及取消归还，并通过服务端结算、状态机与条件更新保障金额和并发一致性。

## 2. 架构支柱与简历候选 bullet

- **用户资产建模：** 针对原有券模型无法表达同一用户多张同规则券和个人动态到期时间的问题，将套餐规则、购买凭证和用户红包实例分层建模，以实例级状态和到期时间支持独立使用与追溯；功能完成度和数据指标待实现后验证。
- **支付幂等治理：** 针对支付回调可重复、调起支付不等于到账的问题，设计独立购买单状态流，经验签、身份和金额校验后在单事务内发放，再以唯一约束防止超发；幂等效果待重复回调测试证明。
- **服务端计价：** 针对客户端金额和红包标识可被篡改的问题，在结算预览与最终提交阶段均以服务端购物车、商品价格和用户红包资产重新计算，并保存实付和减免快照，使支付金额可验证、历史订单可解释。
- **并发状态管理：** 针对双击或并发请求可能重复消费同一红包的问题，以未使用、冻结、已使用和已过期状态建模，并使用带用户、状态与时间条件的原子更新，使同一红包最多被一个订单占用。
- **补偿与过期语义：** 针对订单取消、超时或创建失败后红包可能丢失的问题，将冻结与订单创建纳入同一事务，并在取消链路按当前到期时间决定恢复或过期，使资产状态与订单结果对齐；异常链路效果待故障注入验证。

## 3. 面试问题

### 1. 为什么不直接复用原 user_coupon 表？

**第一人称口播：** 我先核对现有模型，发现用户与券模板存在组合唯一约束，同一用户无法拥有五张相同模板的券；同时有效期在模板上，是所有用户共享时间，不能表达每个人从支付成功起一个月有效。因此我将规则、购买凭证和用户资产分层，每张红包都有独立 ID、状态和到期时间。这样既支持同一用户重复购买，也能逐张冻结、使用、过期和追溯。当前这是设计结论，实际效果要等数据库迁移和集成测试完成后再验证。

**追问：再次购买同一套餐怎么办？** 每次购买都生成独立购买单，再通过购买单和序号的组合唯一键发出五张，不对用户与套餐设置唯一限制。若产品以后要求每月限购一次，我会增加购买周期键并用数据库唯一约束控制，而不是只在前端禁用按钮。这样规则是否允许复购与支付回调是否幂等是两个独立问题，数据结构不会因为临时营销规则而失去扩展性。

### 2. 为什么红包购买不能复用餐品订单？

**第一人称口播：** 餐品订单除了收款，还有接单、制作、配送、完成、库存和销量统计等履约语义；红包购买是数字资产交易，支付后的履约只是发放五张红包。强行共用会让接单列表、营业统计和超时任务出现大量特判，所以我选择独立购买单，只复用微信支付工具、回调验签和通用幂等思想。这样两类业务状态边界清楚，也便于以后分别处理退款、对账和故障恢复。

**追问：回调如何区分业务？** 可以先用明确的订单号前缀路由，验签解密后交给对应处理器，每个处理器再反查本地业务单并校验金额和状态。如果后续增加会员、充值等多种支付场景，我会演进为通用支付单，保存业务类型和业务 ID，由统一支付层完成验真，再调用各自履约处理器。

### 3. 怎样保证重复回调不会多发？

**第一人称口播：** 我会使用三层保护。第一层在事务内锁定购买单，只允许待支付转为已支付；第二层记录并比对交易号、金额和首次回调内容，不一致时报警；第三层给用户红包设置购买单与序号组合唯一约束。支付状态变更和五条资产插入处在同一事务中，任一插入失败都会整体回滚。即使应用代码重复执行，数据库也不会允许出现第六张红包，后续相同回调可以安全返回幂等成功。

**追问：发到第三张时报错怎么办？** 因为状态更新和五条插入在同一事务中，前三张以及购买单状态都会回滚，后续微信重试时可以重新完整处理。还应有主动查单或对账任务找出微信已收款、本地仍待支付的购买单并重放履约；重放继续经过同一幂等服务和唯一约束，不能由运维脚本绕开业务约束直接补数据。

### 4. 为什么回调验签后还要校验金额？

**第一人称口播：** 验签只能证明通知来自微信且内容未被中途修改，不能单独证明该金额就是这笔业务应付的十元。我会同时校验商户号、AppId、业务单号、交易状态、币种和整数分金额，再与购买单保存的价格快照比较。任一项不一致都不发红包并进入告警或对账。套餐后来改价也不会影响历史购买，因为回调校验针对创建购买单时保存的快照，而不是套餐当前价格。

**追问：金额为什么不用 double？** Java 业务层使用 BigDecimal，数据库使用 DECIMAL，与微信交互时转整数分。二进制浮点无法精确表示部分十进制小数，直接比较和元分转换可能出现尾差。所有运算还要明确精度和舍入规则，回调对比订单快照，从源头避免因浮点或规则变化造成金额不一致。

### 5. 为什么要在餐品订单创建时冻结红包？

**第一人称口播：** 创建订单到支付成功之间存在十五分钟窗口，而订单实付已经固定。不冻结的话，同一红包可能被另一个订单使用，第一单回调时就无法解释原减免。因此我会在订单创建事务内同时扣库存、保存金额快照并把红包从未使用改为冻结；支付成功转已使用，取消或超时归还。这样红包可用性、订单金额和库存状态在同一个业务时点确定，不会把冲突拖到外部支付回调阶段。

**追问：冻结中的红包如何展示？** “我的红包”把它显示为待支付订单占用中，并返回关联订单供跳转；它不能出现在新订单的可用列表。取消时在事务中恢复，若当时已过原到期时间则直接转过期。页面展示只是状态解释，真正可用性仍由后端条件更新中的状态和到期时间共同决定。

### 6. 并发使用同一张红包怎么解决？

**第一人称口播：** 我不会使用先查询再普通更新的两步逻辑，因为两个请求都可能先读到可用。我会用一条条件更新，同时限定红包 ID、当前用户、未使用状态和到期时间，只有第一个请求能将其改为冻结并关联订单。第二个请求受影响行数为零，就回滚订单创建并提示重新选择。这条约束在数据库层生效，多实例部署也不依赖单机锁，正确性比在应用内加 synchronized 更可靠。

**追问：还需要分布式锁吗？** 对“一张红包最多一个订单占用”的正确性而言，数据库条件更新已经足够。若将来同一资产冲突率很高，可以在前面加短期锁或请求幂等键减少无效工作，但它们是性能优化，不替代数据库最终约束。我会先观察冲突数、数据库等待时间和接口延迟，再决定是否增加复杂度。

### 7. 一个月有效期怎样算？

**第一人称口播：** 我会先与产品确认是自然月还是固定三十天。当前方案按自然月，从通过校验的支付成功时间开始，用 plusMonths(1) 生成每张红包的到期时间，这能处理大小月、月末和闰年。使用时必须实时检查 expire_time 大于当前时间，定时任务只负责把历史状态整理为已过期，不能成为可用性唯一依据，否则任务延迟会产生到期后仍能使用的窗口。

**追问：为什么不能只靠定时任务？** 任务可能因宕机、调度延迟或数据库故障晚执行。如果业务只检查状态，到期与任务完成之间就会误用。把状态和到期时间同时写进查询和条件更新，即使定时任务停摆也不会破坏核心约束；定时任务只是方便列表展示、统计和清理。

### 8. 默认应选择哪张红包？

**第一人称口播：** 当前五张红包面额和门槛相同，我会选择最早到期的一张，再按实例 ID 稳定排序，以减少旧红包过期浪费。选择策略由后端结算预览返回，前端只负责默认勾选、展示和允许用户取消。以后面额或门槛不同，可以演进为先筛选可用、再最小化实付、最后选择最早过期。策略集中在服务端能避免小程序和其他客户端各自实现后产生口径分叉。

**追问：为什么不让前端直接算？** 前端可以控制交互，但不能成为资产有效性与金额计算的权威来源，因为客户端可被调试篡改且不同版本可能逻辑不一致。最终提交时，后端必须重新读取购物车、红包所有人、状态和到期时间，再计算和冻结，不信任客户端的 amount 与减免值。

### 9. 红包选择为什么放在提交订单页？

**第一人称口播：** 当前小程序的提交订单页负责确认地址、费用和备注，点击去支付后才创建餐品订单；后面的支付页只调微信支付。红包会改变订单实付，所以应在创建订单前展示和确认。订单一旦创建，红包与实付都成为快照，支付页不再改价，从而避免微信预支付单、本地订单金额和回调金额出现不一致，也避免为了切换红包而设计复杂的待支付订单重新计价接口。

**追问：如果支付页必须允许切换呢？** 那就需要一个重新计价接口，在事务内锁订单与新旧红包，归还旧红包、占用新红包并更新金额快照，同时废弃已生成的预支付信息。这个方案并发和对账复杂度更高，首版价值有限，因此我建议在创建订单前完成选择，并在页面明显提供“不使用红包”。

### 10. 预览金额和提交金额不一致怎么办？

**第一人称口播：** 预览是可能过时的读结果，用户确认期间商品会改价、库存会变化，红包也可能到期或被另一个待支付订单占用。真正提交时必须完整重算，只有提交事务生成的金额才是权威。如果与用户最后看到的预览不同，严格模式下客户端携带报价标识，后端返回“金额已更新，请重新确认”，不能静默按更高金额创建订单。这样兼顾一致性和用户知情。

**追问：为什么要保存快照？** 餐品订单保存实付、减免、用户红包实例 ID，明细保存当时单价；红包购买单保存当时套餐售价、数量、面额和有效期。规则或模板修改后，历史交易仍能对账、退款和解释，支付回调也能和创建时应付金额比对，而不是读取今天的配置。

### 11. 红包抵扣哪些费用？

**第一人称口播：** 这是产品规则而不是纯技术细节。结合项目现有优惠逻辑，首版建议只抵菜品和套餐金额，打包费与配送费在优惠后加入。减免取红包面额和菜品金额的较小值，所以商品不足五元时不找零，红包整张核销。这样不需要零元订单直接完成支付的特殊分支，也与现有结算口径相容。页面必须清楚说明适用范围，避免用户以为配送费也可抵扣。

**追问：若要求抵扣所有费用呢？** 需要计算总费用后的实付，并处理实付为零的订单：零元单不能调微信支付，应由后端在权限、库存和红包校验后直接完成支付状态迁移，同时保持与正常支付相同的幂等和通知语义。因为分支扩大明显，我会在产品确认后单独设计和测试。

### 12. 取消订单如何归还红包？

**第一人称口播：** 未支付订单取消时，在已有取消事务中根据订单 ID 找到冻结红包，只允许从冻结状态转出。若当前时间仍早于到期时间，就恢复为未使用并清除订单关联；否则直接改为过期。这个变化与订单取消和商品库存回补一起提交，任一步失败都回滚，避免订单已取消但红包仍锁住。重复取消应识别为幂等结果或状态冲突，不能再次修改一张已经恢复或使用的红包。

**追问：已支付后退款返红包吗？** 这是产品和财务规则。建议等微信退款真正成功后再返，不能用户点取消就立即返，否则现金未退期间红包又可重复用。退款成功时仍比较原到期时间，未到期则恢复，已到期则过期，默认不延长；若需要补偿用户，应另发有独立来源的补偿红包。

### 13. 当前异步 MQ 下单有什么风险？

**第一人称口播：** 当前接口发送消息后就返回订单号和预计金额，真正订单、库存与券在消费者中落库。如果两个消息指定同一红包，只有一个能最终冻结成功，但另一个客户端可能已经跳到支付页。因此首版可以把订单核心落库改为同步事务，成功后再返回；或者在接口阶段先预占红包并设计可靠消息和补偿。对这个体量的功能，同步落库更容易保证用户看到的订单一定存在且金额已锁定，再把通知等非核心动作异步化。

**追问：为什么不把所有步骤都异步化？** 异步能削峰，但会引入接受请求与业务成功之间的中间状态，需要查询结果、幂等键、超时补偿和用户提示。红包与库存属于创建订单的强一致核心，直接异步会让支付页面出现订单未落库、红包未冻结等短暂不一致。应根据真实吞吐量和延迟基线决定，而不是为了使用 MQ 而扩大一致性成本。

### 14. 最重要的测试有哪些？

**第一人称口播：** 我会按规则、并发和支付异常组织测试。规则层覆盖发五张、默认最早到期、不使用、商品金额小于面额以及月末到期；并发层让多个订单同时占用同一红包，断言只有一个成功；支付层重放回调、伪造金额、模拟发放中途失败并验证事务回滚。取消、超时、支付成功和退款还要覆盖红包状态迁移。测试不仅看接口，还直接断言数据库中一笔购买恰好五张且金额快照一致。

**追问：哪个集成用例优先级最高？** 我会优先做重复回调叠加中途失败：创建十元待支付购买单，对同一交易并发重放通知，并在首次插入过程中注入异常。预期失败事务无半成品，重试后最终恰好五张；改金额或交易号再重放则应报警而不是静默成功。这一条能同时验证事务、幂等和唯一约束。

### 15. 上线后如何发现少发或多发？

**第一人称口播：** 我会按购买单对账：每笔已支付的指定套餐购买单都应恰好对应五张用户红包，并且用户、面额、领取时间和到期规则与购买快照一致；未支付或关闭单不应有资产。定时对账发现差异后记录单号、交易号和实际张数并报警，自动补发也必须走同一幂等发放服务。除此之外监控购买创建数、支付成功数、发放张数、占用冲突、归还失败和过期数，真实阈值在获得运行基线后设定。

**追问：能直接写 SQL 补发吗？** 不建议。直接写 SQL 容易绕过所有人、到期时间、序号唯一键、审计和状态规则，还可能在回调随后重试时产生冲突。应该提供受控的重放或补偿入口，输入购买单号，重新校验支付凭证和当前资产数，再调用幂等发放服务，并记录操作人、原因和结果，确保补偿本身也可追溯。

## 4. 源码证据索引

| 主题 | 路径与符号 | 对应问题 |
| --- | --- | --- |
| 现有券表限制 | sky-server/src/main/resources/db/migration/V20260622__production_features.sql，uk_user_coupon | 1 |
| 用户券实体 | sky-pojo/src/main/java/com/sky/entity/UserCoupon.java | 1、7 |
| 现有领券 | sky-server/src/main/java/com/sky/service/impl/CouponServiceImpl.java，receive | 1 |
| 服务端计价 | sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java，calculateDiscount | 8、10、11 |
| 现有券占用 | OrderServiceImpl.java，markCouponUsed；UserCouponMapper.java，markUsed | 5、6 |
| 取消返券 | OrderLifecycleService.java，cancelLocked；UserCouponMapper.java，rollbackByOrderId | 12 |
| 支付金额 | OrderServiceImpl.java，payment | 4、9 |
| 回调验真 | sky-server/src/main/java/com/sky/controller/notify/PayNotifyController.java | 3、4 |
| 支付状态幂等 | sky-server/src/main/java/com/sky/service/impl/OrderLifecycleService.java，paid | 3、14 |
| 提交与支付页 | ../mp-weixin/pages/order/index.*；../mp-weixin/pages/pay/index.js | 9 |

## 5. 交给 /asu 的项目事实摘要

- 项目名称与方向：智餐外卖红包模块，Java 后端为主。
- 当前职责边界：已完成仓库分析和模块设计；实现、测试、上线尚未发生。
- 技术动作：资产分层、独立购买单、支付幂等、条件更新占用、服务端重算、取消归还。
- 可核验证据：仓库已有券表、订单优惠、支付回调验签和取消返券骨架；新红包表和接口尚未落地。
- 简历候选：只有实现并通过测试后，才可从第 2 节选取。
- 待补指标：回调重放结果、并发占用结果、接口 P95、默认使用率与转化数据。

## 6. 交给 /interview 的高风险 Claim

- Ownership：当前不能说“主导上线”，只能说完成设计。
- Metric：没有用户数、转化、延迟或收益数据，不可编造。
- Architecture：可讲三层资产模型、冻结状态机和幂等设计，不能说已生产验证。
- Result：不能说已发布、已采用或提升营业额，当前结果是可审阅的设计文档。
