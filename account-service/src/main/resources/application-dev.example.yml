# 复制为 application-dev.yml 后替换所有占位符。application-dev.yml 不入库。
spring:
  datasource:
    druid:
      username: your_mysql_username
      password: your_mysql_password
sky:
  wechat:
    # 小程序 appid / secret，必须与实际运行的小程序和 jscode2session 调用方一致
    appid: your_wechat_appid
    secret: your_wechat_secret
