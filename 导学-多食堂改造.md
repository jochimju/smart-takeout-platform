# 多食堂改造导学

> 范围：基于 `sky-take-out-jjs` 后端、根目录 `mp-weixin` 已编译小程序与管理端源码的架构设计。本文是改造方案，不表示功能已实现。

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| 多租户/数据域隔离 | 避免菜品、订单、员工串食堂 | Controller、Service、Mapper 查询条件 | 高 |
| 领域建模 | 决定食堂、档口、分类、商品的归属 | `canteen`、`category`、`dish`、`setmeal` | 高 |
| 订单快照 | 名称、电话修改后历史订单仍可追溯 | `orders` | 高 |
| 服务端业务校验 | 前端传入的食堂 ID 不能作为可信事实 | 购物车、下单、秒杀服务 | 高 |
| Redis 缓存命名空间 | 多食堂后需要按业务域失效 | `MenuCache`、营业状态 | 中高 |
| RBAC + 数据权限 | 接口权限不等于数据范围权限 | 管理端 JWT、员工关系 | 高 |
| 兼容性迁移 | 现有数据和客户端不能一次性失效 | Flyway、接口版本、回填 | 中高 |

## 2. 重点亮点与学习顺序（先看这个）

| 亮点标题 | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 建议顺序 |
| --- | --- | --- | --- | --- |
| 业务边界建模 | 食堂是下单和履约的最小单元 | 聚合根、外键、快照 | `db/migration/V20260909__campus_dining_schema.sql` | 1 |
| 全链路数据隔离 | 仅在菜品表加 ID 不能防止串店 | 数据域、服务端校验 | `CategoryServiceImpl.java`、`ShoppingCartServiceImpl.java`、`OrderServiceImpl.java` | 2 |
| 同店购物车 | 一个订单不应跨食堂 | 不变式、并发校验、冪等 | `ShoppingCartMapper.xml`、`OrderServiceImpl.java` | 3 |
| 缓存与营业状态分区 | 当前是全局营业开关 | 缓存键设计、局部失效 | `MenuCache.java`、两个 `ShopController.java` | 4 |
| 管理端数据权限 | 食堂管理员只能处理自己食堂 | RBAC、scope、纵深防御 | `JwtTokenAdminInterceptor.java`、`PermissionMapper.java` | 5 |

## 3. 必备知识点

- [ ] 食堂是不是等于当前的“商家/门店”，是否还有档口层。
- [ ] 跨食堂是否允许合并下单；本方案默认不允许。
- [ ] 配送费、起送价、营业时间、红包、秒杀是全局还是按食堂。
- [ ] 管理员是单食堂还是可管理多食堂。
- [ ] 订单下单时保存食堂快照，不仅保存外键。

## 4. 推荐阅读（结合仓库）

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- | --- |
| 旧校园模型 | 表关系与迁移完整性 | `sky-server/src/main/resources/db/migration/V20260909__campus_dining_schema.sql` | 25 分钟 | 为什么它还没形成多食堂闭环 |
| 菜单查询 | 查询条件和缓存 | `sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java` | 20 分钟 | 菜品如何按分类返回 |
| 购物车 | 同店不变式 | `sky-server/src/main/java/com/sky/service/impl/ShoppingCartServiceImpl.java` | 20 分钟 | 为什么现在会混入多店商品 |
| 下单链路 | 订单归属、服务端重算 | `sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java` | 35 分钟 | 应在哪里校验食堂一致性 |
| 管理侧权限 | 功能权限与数据权限 | `sky-server/src/main/java/com/sky/interceptor/JwtTokenAdminInterceptor.java` | 20 分钟 | 为什么现有 RBAC 还不足够 |
| 小程序菜单页 | 页面状态与 API 传参 | `../mp-weixin/pages/index/index.wxml`、`../mp-weixin/common/vendor.js` | 30 分钟 | 客户端需要新增哪些状态 |

## 5. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。

## 6. 项目技术定位

前后端交叉改造：Spring Boot/MyBatis 后端需要增加食堂业务域和数据权限，小程序需要新增食堂选择与上下文，Vue 管理端需要按食堂管理数据。

## 7. 核心原理解析

### 7.1 食堂是下单边界

问题：现有购物车和订单只按用户聚合。机制：用 `restaurant_id` 贯通商品、购物车和订单，下单时反查每个商品的真实归属。落点：服务端保证一车一店、一单一店。

### 7.2 外键与快照同时存在

问题：历史订单不能随食堂改名而改变。机制：订单保存 `restaurant_id` 用于关联，同时保存名称、联系电话等下单时快照。落点：订单详情和售后按快照展示。

### 7.3 用户选择不等于安全边界

问题：用户可以篡改 `restaurantId`。机制：菜品和套餐从数据库反查归属，购物车加车与下单均做一致性校验。落点：禁止串店商品、越权订单和错误优惠结算。

### 7.4 管理端是数据权限问题

问题：当前只验证员工能否访问某接口。机制：增加员工与食堂关系，查询和写入都强制带 scope。落点：超级管理员可选食堂，食堂管理员的 scope 由服务端确定。

### 7.5 分区缓存和失效

问题：菜单与营业状态已不再全局共享。机制：使用 `restaurant:{id}:...` 命名空间，按食堂进行读取与失效。落点：一家食堂修改菜单不影响其他食堂的热数据。

## 8. 关键设计决策

| 备选 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- |
| 方案 A：只给菜品/套餐加食堂 ID | 上线快，改动小 | 分类、车、单、权限仍会串店 | 仅适合演示，不建议正式实现 |
| 方案 B：食堂作为门店聚合根 | 改造量中等，建模清晰 | 需要贯通所有主链路 | 推荐；做全链路集成测试 |
| 方案 C：食堂-档口两级 | 最贴近校园真实组织 | 调度、结算、拆单复杂度显著增加 | 只在确认需要档口独立接单时选用 |

### 推荐的数据关系

`restaurant 1-N category`，`restaurant 1-N dish`，`restaurant 1-N setmeal`，`restaurant 1-N shopping_cart`，`restaurant 1-N orders`，`employee N-N restaurant`。菜品和套餐同时保存 `restaurant_id` 是有意识的冗余：它使归属校验和分店查询直接，但写入时必须验证分类与商品的食堂一致。

## 9. 量化与验证（待测，建议）

- 待测：6 个食堂各至少准备 2 个分类、1 个套餐和 3 个菜品，校验列表不串店。
- 待测：同一用户并发从两个食堂加车，服务端只允许一个食堂的购物车成立。
- 待测：篡改食堂 ID、菜品 ID、套餐 ID，下单均应被拒绝且不扣库存。
- 待测：一家食堂关闭或更新菜单，其他食堂缓存和营业状态不受影响。
- 待测：食堂管理员直接传入其他食堂 ID，所有查询、修改和订单操作均无权。
- 待测：完成旧数据回填后，旧订单可查、旧菜品可管理，新接口不接受空食堂归属。
