# 智餐外卖红包模块：设计与落地导学

> 目标：用户支付 10 元购买红包包，支付成功后获得 5 张 5 元红包；每张红包归属购买用户，从支付成功时起一个自然月内有效。提交餐品订单时默认使用一张可用红包，用户可以选择不使用，实付金额始终由后端计算。

## 1. 前置知识

| 知识点 | 为何需要 | 项目位置 | 高频度 |
| --- | --- | --- | --- |
| 数据库事务与条件更新 | 防止一张红包被并发使用两次 | OrderServiceImpl、UserCouponMapper | 高 |
| 支付回调幂等 | 微信可能重复通知，不能重复发 5 张 | PayNotifyController、OrderLifecycleService | 高 |
| 金额快照 | 规则变化后仍可解释历史订单 | orders.amount、discount_amount | 高 |
| 资产状态机 | 区分可用、冻结、已用、过期 | 新增用户红包表 | 高 |
| 时间边界 | 一个月不等于固定 30 天 | 支付成功时间 plusMonths(1) | 中 |
| 服务端结算 | 防止前端篡改金额与红包 ID | OrderServiceImpl | 高 |

## 2. 重点亮点与学习顺序

| 亮点 | 为什么重要 | 通用关键词 | 先看文件 | 顺序 |
| --- | --- | --- | --- | --- |
| 用户资产建模 | 5 张红包必须是 5 个独立实例 | 模板、交易、资产 | db/migration/V20260622__production_features.sql | 1 |
| 支付幂等发放 | 重复回调不能多发 | 行锁、状态机、唯一约束 | PayNotifyController.java | 2 |
| 服务端计价 | 支付金额必须可信 | 快照、防篡改 | OrderServiceImpl.java | 3 |
| 红包冻结与归还 | 待支付期间不可重复用，取消后可归还 | 条件更新、事务 | OrderLifecycleService.java | 4 |
| 默认选中交互 | 满足自动使用又允许不用 | 结算预览、默认策略 | ../mp-weixin/pages/order/index.* | 5 |

## 3. 仓库现状与结论

项目已经有 coupon、user_coupon、订单减免和取消返券的骨架，可以复用思路，但不能只改显示名称：

- user_coupon 当前有 (user_id, coupon_id) 唯一约束，同一用户无法拥有 5 张相同模板的券。
- 有效期目前在 coupon 模板上，是所有用户共享的活动时间，不是购买成功后各自一个月。
- 订单当前传入 couponId，实际指向券模板；多张同模板红包必须传用户红包实例 ID。
- 现有订单取消会返券，这是红包冻结/归还链路的良好基础。
- 现有支付回调已经做验签、商户身份、币种和金额校验，可复用安全边界。

推荐把数据库和后端统一改成“红包”语义，并采用“红包套餐—购买单—用户红包实例”三层模型。

## 4. 数据库设计

### 4.1 red_packet_package（红包套餐）

保存可销售规则，不表示用户资产：

| 字段 | 示例 | 说明 |
| --- | --- | --- |
| id | 1 | 主键 |
| name | 10元红包包 | 套餐名 |
| sale_price | 10.00 | 售价 |
| packet_count | 5 | 发放张数 |
| packet_amount | 5.00 | 每张面额 |
| valid_months | 1 | 有效自然月数 |
| status | 1 | 上下架状态 |
| create_time/update_time | 时间 | 审计字段 |

### 4.2 red_packet_purchase_order（红包购买单）

红包购买不能伪装成餐品订单，否则会污染接单、配送、销量与营业统计。

主要字段：id、order_no、user_id、package_id、pay_amount、packet_count_snapshot、packet_amount_snapshot、valid_months_snapshot、status、transaction_id、create_time、paid_time。

关键约束：

- order_no 唯一；
- transaction_id 唯一，未支付时允许为空；
- 回调只允许 PENDING 转为 PAID；
- 购买单保存套餐快照，回调不能读取可能已改价的当前套餐来验金额。

### 4.3 user_red_packet（用户红包实例）

主要字段：id、user_id、package_id、purchase_order_id、sequence_no、amount、status、receive_time、expire_time、used_time、food_order_id。

状态建议：

- 0 UNUSED：可用；
- 1 RESERVED：已被待支付餐品订单冻结；
- 2 USED：餐品订单支付成功，已核销；
- 3 EXPIRED：已过期。

关键约束与索引：

- (purchase_order_id, sequence_no) 唯一，保证一次购买恰好最多发 5 张；
- food_order_id 唯一且可为空，保证一张餐品订单最多用一张；
- (user_id, status, expire_time, amount) 联合索引，支持可用红包查询。

### 4.4 orders 改造

新增 user_red_packet_id，保留 discount_amount 作为减免快照。原 coupon_id 可以暂时保留兼容旧数据，新红包逻辑不再依赖它。

## 5. 核心流程

### 5.1 购买与发放

1. 用户查看红包套餐，后端返回“10 元、5 张、每张 5 元、一个月有效”。
2. 创建红包购买单，后端从套餐生成价格和规则快照。
3. 调用现有微信支付工具，以购买单号支付 10 元。
4. 微信回调完成验签、商户号、AppId、交易状态、币种、单号和 10 元金额校验。
5. 事务内锁定购买单，只允许首次 PENDING 转 PAID，并插入 sequence_no 为 1～5 的五条用户红包。
6. receive_time 使用可信的支付成功时间，expire_time 使用 receive_time.plusMonths(1)。
7. 重复回调返回幂等成功；若交易号或金额与首次不同，则记录冲突并报警。

支付成功状态和五条红包必须在同一数据库事务中提交。数据库唯一约束是重复回调时的最后一道防线。

### 5.2 结算页默认使用

当前小程序 pages/order 是创建订单前的“提交订单”页，pages/pay 只负责调起微信支付。因此红包选择应放在 pages/order，而不是微信支付按钮点击之后。

进入提交订单页时调用结算预览接口。后端根据登录用户与当前购物车返回：

- 商品原价；
- 打包费、配送费；
- 可用红包列表；
- 默认红包实例 ID；
- 减免金额与预计实付。

默认策略：优先选择最早过期的一张；将来面额不同时，可升级为“先最小化实付，再选最早过期”。页面展示“红包 -¥5.00”，点击后可改选其他红包或“不使用红包”。不使用时提交 null。

预览只是展示。最终提交时后端必须重新读取购物车、当前价格和红包状态并重新结算，绝不使用前端传入的 amount。

### 5.3 下单、冻结与支付

餐品订单创建事务内执行：

1. 校验红包属于当前用户、状态为 UNUSED 且 expire_time 大于当前时间；
2. 用一条条件更新把红包改成 RESERVED 并写入 food_order_id；
3. 重新计算商品金额和减免；
4. 扣商品库存，保存订单、明细、红包减免与实付快照；
5. 任一步失败则整笔事务回滚。

支付页只能展示订单快照并用 orders.amount 调起微信支付，不再动态改价。餐品支付回调成功后将 RESERVED 改为 USED。

取消或 15 分钟超时时，将 RESERVED 红包归还。若归还时已经到期，则改为 EXPIRED，不能恢复为 UNUSED。

## 6. 金额规则

首版建议红包只抵扣餐品金额，不抵扣打包费和配送费，与当前优惠计算口径一致：

    discount = min(redPacketAmount, foodAmount)
    payable = foodAmount - discount + packFee + deliveryFee

这样商品不足 5 元时不找零，红包整张核销；实付仍包含配送费和打包费，也不需要增加零元订单分支。这个产品规则应在页面写清楚。

如果产品以后要求抵扣所有费用，需额外设计 0 元单：0 元时不应再调微信支付，而应由后端直接完成支付状态迁移。首版不建议扩大到该分支。

## 7. 接口设计

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | /user/red-packet/package | 查询可购买套餐 |
| POST | /user/red-packet/purchase-orders | 创建待支付购买单 |
| PUT | /user/red-packet/purchase-orders/payment | 调起购买单微信支付 |
| GET | /user/red-packets?status=AVAILABLE | 查询我的红包 |
| GET | /user/order/checkout | 返回结算预览、可用列表和默认红包 |
| POST | /user/order/submit | 传 userRedPacketId；为空表示不使用 |

支付回调可按订单号前缀路由到餐品订单或红包购买单处理器。若以后还会增加会员、充值等支付类型，建议演进为通用 payment_order 表，通过业务类型和业务 ID 路由。

## 8. 关键设计决策

| 决策 | 备选 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| 独立红包购买单 | 复用餐品订单 | 多一套轻量状态，但不会污染配送业务 | 回调路由错误 | 两类支付集成测试 |
| 创建餐品订单时冻结 | 支付成功时再扣 | 占用最多 15 分钟，但金额稳定 | 取消漏归还 | 取消、超时、异常回滚测试 |
| 最早过期默认选中 | 任意一张 | 当前面额相同，可减少过期浪费 | 策略不透明 | UI 明示“自动使用” |
| 查询时实时判断过期 | 只靠定时任务 | 没有任务延迟误用窗口 | 时区偏差 | 统一 DB/JVM 时区 |
| plusMonths(1) | 固定 30 天 | 符合自然月语义 | 产品理解不同 | 覆盖月末、闰年用例 |

## 9. 异常边界

1. 未支付红包购买单不发任何红包。
2. 相同微信回调重复十次，用户仍只有 5 张。
3. 回调金额不是 10 元，拒绝发放并进入告警/对账。
4. 用户传入别人的红包实例 ID，拒绝创建订单。
5. 两个并发订单选同一张，仅一笔条件更新成功。
6. 扣库存或写订单失败，红包冻结一并回滚。
7. 未支付订单取消/超时，红包恢复；此时已到期则直接过期。
8. 餐品支付成功，冻结转已使用；重复回调不重复核销。
9. 已支付餐品订单退款是否返红包必须有产品规则。建议退款成功后返；已过原有效期则不延期。

## 10. 推荐阅读

| 主题 | 技术点 | 相对路径 | 时间 | 读完能回答 |
| --- | --- | --- | --- | --- |
| 券表限制 | 模板和实例 | sky-server/src/main/resources/db/migration/V20260622__production_features.sql | 15 分钟 | 为什么现表不能发 5 张 |
| 领取流程 | 库存条件更新 | sky-server/src/main/java/com/sky/service/impl/CouponServiceImpl.java | 15 分钟 | 如何防超发 |
| 订单计价 | 后端重算、快照 | sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java | 30 分钟 | 如何防金额篡改 |
| 取消链路 | 事务、库存与返券 | sky-server/src/main/java/com/sky/service/impl/OrderLifecycleService.java | 25 分钟 | 如何归还红包 |
| 回调验真 | 验签和金额校验 | sky-server/src/main/java/com/sky/controller/notify/PayNotifyController.java | 25 分钟 | 何时可以发红包 |
| 小程序流程 | 创建订单与支付分层 | ../mp-weixin/pages/order/index.*、../mp-weixin/pages/pay/index.* | 20 分钟 | 红包选择放在哪 |

## 11. 量化与验证

- 功能：支付后恰好 5 张，重复回调后仍为 5 张。
- 一致性：并发使用同一红包，成功冻结数必须为 1。
- 金额：支付回调金额必须等于购买单或餐品订单保存的实付。
- 时间：覆盖到期前 1 秒、到期时刻、到期后 1 秒以及 1 月 31 日和闰年。
- 性能：结算预览和提交订单的 P95、SQL 次数待测；确认可用红包索引命中。
- 观测：记录购买成功数、发放张数、购买与发放不一致数、冻结冲突数、过期数，真实基线待测。

## 12. 简洁 TodoList

- [ ] 新增红包套餐、购买单、用户红包三张表，并给 orders 增加用户红包实例 ID。
- [ ] 实现套餐查询、创建购买单、微信支付和回调幂等发 5 张。
- [ ] 实现“我的红包”和订单结算预览，默认选择最早过期红包。
- [ ] 改造订单提交：后端重算金额、事务内冻结红包、保存减免快照。
- [ ] 完善支付核销、取消/超时归还和过期判断。
- [ ] 小程序增加红包购买页、我的红包、结算页默认选中与“不使用红包”。
- [ ] 补齐回调幂等、并发占用、金额防篡改、过期边界和取消归还测试。

## 13. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。
