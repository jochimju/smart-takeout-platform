# 智餐外卖五服务架构改造 TODO

> 实施范围：`sky-take-out-jjs`  
> 设计依据：[智餐外卖微服务架构改造设计方案](./智餐外卖微服务架构改造设计方案.md)  
> 当前状态：五服务代码改造、数据迁移、构建测试和本地运行验收已完成；真实外部平台项目仍需有效凭据人工验收。

## 1. 已完成的基础改造

- [x] 升级 Java 17、Spring Boot 3.5、Spring Cloud、Spring Cloud Alibaba 和 MyBatis-Plus。
- [x] 新建 `gateway-service`，接入 Nacos、Gateway、Redis 限流和 Trace ID。
- [x] 新建 `account-service`，迁移用户、员工、权限和收货地址。
- [x] 交易侧通过 OpenFeign 查询账户服务，不再访问账户表。
- [x] 当前 `sky-server` 以 `commerce-service` 注册并运行。
- [x] 完成三个服务的构建、健康检查、网关转发和双实例验证。

## 2. 拆分前准备

- [ ] 固定商品、购物车、订单、秒杀、支付和 WebSocket 的现有接口结果。
- [ ] 新建 `sky-contracts` 模块，迁入商品内部 DTO、通知事件 DTO 和事件公共字段。
- [ ] 整理商品表、交易表、Redis 键、RabbitMQ 交换机和 Flyway 脚本归属。
- [ ] 补充商品报价、订单快照、通知事件幂等所需的测试。

**完成标准：**跨服务只共享契约 DTO，不共享 Mapper；拆分前后的业务结果可以对比。

## 3. 新建商品服务 `catalog-service`

- [ ] 创建 `catalog-service` Maven 模块、启动类、配置文件和 Nacos 注册配置，默认端口 `8083`。
- [ ] 迁移分类、菜品、口味、套餐、套餐菜品相关 Controller、Service、Mapper 和 XML。
- [ ] 迁移商品图片、商品缓存、缓存失效和缓存预热逻辑。
- [ ] 创建独立商品数据库和 Flyway 脚本，迁移并核对商品数据。
- [ ] 商品服务成为商品表的唯一写入方，交易服务删除商品 Mapper 和跨库 SQL。
- [ ] 提供批量商品报价、套餐组成和商品快照内部接口。
- [ ] 将商品公开路径从网关切换到 `catalog-service`。

**完成标准：**分类、菜品和套餐接口由商品服务独立提供；商品新增、修改、上下架和缓存行为与原系统一致。

## 4. 调整交易服务的商品依赖

- [ ] 在交易库建立交易库存表或库存账本，库存扣减继续使用本地事务。
- [ ] 普通下单、秒杀下单改为在事务前批量查询商品报价和状态。
- [ ] 订单明细保存名称、价格、图片、规格等成交快照。
- [ ] 购物车展示使用商品批量查询，避免逐条调用 Feign。
- [ ] 商品服务不可用或报价版本变化时返回明确的可重试错误。
- [ ] 验证重复提交、库存扣减、优惠券、红包和秒杀的一致性。

**完成标准：**交易服务不直接访问商品表；订单和库存仍在一个交易数据库事务中完成。

## 5. 新建通知服务 `notification-service`

- [ ] 创建 `notification-service` Maven 模块、启动类、配置文件和 Nacos 注册配置，默认端口 `8084`。
- [ ] 将 WebSocket 服务及连接管理代码迁入通知服务。
- [ ] 定义订单提交、支付成功、订单状态变化、催单和退款成功事件。
- [ ] 在交易服务中增加 Outbox 事件记录，并通过 RabbitMQ 可靠发布。
- [ ] 配置通知交换机、队列、重试队列和死信队列。
- [ ] 通知消费者按 `eventId` 幂等处理，失败记录可查询和重放。
- [ ] 使用 Redis Pub/Sub 向所有通知实例广播 WebSocket 消息。
- [ ] 将网关 `/ws/**` 路由切换到 `notification-service`。
- [ ] 删除交易服务中的直接 WebSocket 推送实现。

**完成标准：**通知失败不回滚订单或支付；两个通知实例均能向自己持有的连接推送消息，重复事件不会产生不可接受的重复通知。

## 6. 收敛并改名交易服务

- [ ] 将 `commerce-service` 的服务名调整为 `trade-service`，默认端口继续使用 `8081`。
- [ ] 将 `sky-server` 模块整理或重命名为 `trade-service`。
- [ ] 清理已经迁出的商品 Controller、Service、Mapper、缓存和 WebSocket 代码。
- [ ] 保留购物车、订单、库存、优惠券、红包、秒杀、支付、退款、报表和可靠性任务。
- [ ] 检查所有定时任务在双实例下使用租约、条件更新或唯一约束。
- [ ] 更新网关路由、Feign 服务名、Nacos 配置和启动文档。

**完成标准：**交易模块只保留交易职责；服务内本地事务、订单幂等、支付幂等和 MQ 可靠性不被破坏。

## 7. 数据和安全边界

- [ ] 为账户库、商品库、交易库分别创建数据库账号和最小权限。
- [ ] 确认一个业务表只有一个服务拥有写权限。
- [ ] 内部 Feign 请求继续使用内部令牌并透传 Trace ID。
- [ ] 网关清除外部伪造的内部身份头，不公开内部接口。
- [ ] 密码、支付私钥、OSS 密钥只通过环境变量或密钥系统注入。
- [ ] 分服务整理 Flyway 历史，验证全新数据库和已有数据库都能升级。

**完成标准：**任何服务都不能直接写入其他服务的数据表，敏感配置不进入代码仓库。

## 8. 最终五服务回归

- [ ] 五个服务分别构建并启动，全部注册到 Nacos，健康检查为 `UP`。
- [ ] 管理端完成登录、员工、分类、菜品、套餐、订单和报表回归。
- [ ] 小程序完成登录、地址、菜单、购物车、普通下单和订单查询回归。
- [ ] 验证秒杀不超卖、红包和优惠券不重复使用、重复请求不重复建单。
- [ ] 验证支付重复回调、超时取消、退款和 MQ 重复消息。
- [ ] 验证商品服务和账户服务故障时，交易服务能够快速失败并返回明确结果。
- [ ] 验证两个交易实例和两个通知实例下的定时任务、负载均衡及 WebSocket。
- [ ] 验证图片、上传、API 文档、HTTPS/WSS 和前端环境地址。
- [ ] 更新 README、接口清单、启动说明、验收记录和回滚步骤。

**完成标准：**最终运行服务为 `gateway-service`、`account-service`、`catalog-service`、`trade-service`、`notification-service`，原管理端和小程序无需修改业务调用方式。

## 9. 需要真实环境人工验收

- [ ] 微信真实登录和支付回调验签。
- [ ] 微信真实退款。
- [ ] OSS 文件上传。
- [ ] 小程序合法 HTTPS/WSS 域名。
- [ ] 生产监控平台告警接收。

这些项目依赖真实外部凭据和生产配置，不能使用本地模拟结果代替。
