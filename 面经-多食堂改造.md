# 多食堂改造面经

> 说明：当前是架构设计，未实现、未上线、未测量。因此下文只使用“设计/计划/建议”，不将结果冒充为已完成经历。

## 1. 项目简介（简历可用，实现后再使用）

设计将单店校园外卖系统演进为六食堂模式，以食堂作为菜单、购物车、订单、营业状态和管理权限的业务边界，规划完成小程序、管理端与后端的全链路隔离。

## 2. 简历 bullet（实现后按真实责任改写）

- **业务边界建模：** 针对原系统默认只有一个商家、菜单与订单无食堂归属的问题，设计以食堂为聚合边界的数据模型，使分类、商品、购物车和订单拥有可校验的归属；实现成果与性能待测。
- **业务不变式：** 针对多食堂商品可能在同一用户购物车混合的问题，设计“一车一店、一单一店”约束，在加车和下单两个边界反查商品归属，防止只依赖客户端状态；并发效果待测。
- **数据权限治理：** 针对原有权限只能判断员工能否访问接口、无法限制查看哪家食堂数据的问题，设计员工与食堂的授权关系，将数据 scope 下沉到查询和写入条件；越权防护待安全测试验证。
- **缓存分区治理：** 针对菜单缓存和营业开关按单店设计的问题，设计按食堂划分的缓存键和局部失效机制，使单个食堂的更新与停业不必清除全局热数据；命中率和失效成本待测。
- **兼容性迁移：** 针对仓库已有校园餐饮数据库草案但未贯通 Java 模型和主链路的状态，设计“建表与可空列—数据回填—双读兼容—收紧约束”的分步迁移，历史数据可用性需通过回归测试验证。

## 3. 面试问题（设计阶段口播）

### 主问 1：为什么不是只在菜品表加一个食堂 ID？

**第一人称口播：** 我先从业务闭环而不是页面展示出发。菜品有食堂 ID 只能过滤菜单，但分类仍可串店，套餐仍无归属，购物车仍可混入两家食堂商品，订单也不知道应由谁接单。因此我把食堂定义为下单与履约的聚合边界，计划让分类、菜品、套餐、购物车和订单全部带有可校验归属，再在服务端维护一车一店的不变式。

- **追问：食堂 ID 是否一定要冗余到菜品表？** 我会把这看成查询与一致性的权衡。从规范化看，菜品可以通过分类间接得到食堂；但菜单、加车和管理端都频繁按食堂查询，直接字段更清晰。如果选择冗余，我会在写入服务中强制校验菜品与分类的食堂一致，并用集成测试防止两个字段漂移，不会只为少写一个联表就牺牲可维护性。
- **追问：如果未来增加档口怎么办？** 我会先确认档口是纯展示分组，还是拥有独立营业、库存、接单和结算能力。前者只需在食堂下加分组，不应过早引入拆单；后者则应把档口升级为更小的履约单元，订单可能需要主单与子单。当前用户明确的只是六个食堂，所以我建议先做一级模型，保留后续扩展点而不预付复杂度。

### 主问 2：如何防止跨食堂购物车？

**第一人称口播：** 我会同时在交互和服务端处理。小程序在用户从新食堂加车时提示“清空原购物车并切换”，但这不是安全保证。后端会反查菜品或套餐的真实食堂，再与用户现有购物车的食堂比较；不一致就拒绝或由明确的“切店”接口在事务中清空后加入。下单时再校验一次，避免并发请求绕过加车检查。

- **追问：为什么下单时还要重复校验？** 我不会假设购物车在下单前始终没被改变。用户可能在两台设备或两个并发请求中加车，商品也可能被管理员转移、下架或停业。下单是扣库存和产生履约责任的最后信任边界，因此要在同一事务中重新读取购物车、校验食堂一致性与营业状态，再扣库存并落单，以最终状态为准。
- **追问：是拒绝还是自动清车？** 我建议默认拒绝，由客户端给用户一次明确确认，然后调用专门的切换购物车操作。如果后端在普通加车请求中自动清车，网络重试或误触都可能让原购物车无提示消失。专门操作可以携带目标食堂和客户端请求号，在事务中完成校验、清车和加车，并对重试保持冪等。

### 主问 3：菜单 API 如何设计？

**第一人称口播：** 我倾向把食堂放在资源路径中，例如先查食堂列表，再访问某食堂下的分类、菜品和套餐。这比完全依赖请求头或本地存储更显式，日志、限流和缓存也容易按食堂维度分析。为了平滑迁移，我可以暂时保留原有接口，将其映射到默认食堂，但新小程序必须显式传递已选食堂，且后端验证该食堂是否启用。

- **追问：食堂 ID 放路径、参数还是请求头？** 我会按资源语义选择。菜单明确属于某个食堂，因此路径参数最直观，也有利于 CDN、网关和日志聚合。请求头更适合一次会话中的默认上下文，但隐式上下文容易导致调试和缓存错误。查询参数可用于旧接口兼容，长期我会选择明确的资源路径，并在服务端统一做存在性和状态校验。
- **追问：如何兼容旧小程序？** 我会先建立一个默认食堂并回填旧数据，原接口在未携带食堂时仅返回默认食堂内容，同时为新小程序提供显式多食堂接口。过渡期会记录旧接口调用量，确认客户端已升级后再下线兼容逻辑。我不会让“未传 ID”直接变成全食堂查询，因为那会把兼容性变成串店隐患。

### 主问 4：订单为什么要保存食堂快照？

**第一人称口播：** 我区分可变的主数据和不可被后续修改影响的交易事实。食堂名称、联系电话、位置、配送规则都可能改变，如果历史订单只联表查当前值，用户看到的历史凭证就会被改写。因此我会同时保存食堂外键和必要快照：外键用于运营统计和权限归属，快照用于展示、售后和对账。快照只保存交易需要的字段，不复制整行主数据。

- **追问：删除食堂后历史订单怎么办？** 我建议食堂采用停用而不是物理删除，因为它已经成为订单、结算和审计的归属主体。历史订单通过快照继续展示当时名称和联系信息，外键则指向已停用的记录，便于统计和追责。如果法规或隐私要求必须删除某些信息，我会针对个人数据做匿名化，而不是删掉交易主体关系。
- **追问：快照会不会导致数据重复？** 快照确实是有意识的反规范化，但我会限制它的边界。只复制需要被固定在交易时点的少量字段，例如食堂名称和联系电话，不复制状态、排序和运营配置。订单创建服务在事务内从服务端读取主数据并写入快照，不接受客户端传入名称，这样重复是可控且有审计价值的。

### 主问 5：营业状态如何从全局开关改成多食堂？

**第一人称口播：** 现有系统在 Redis 中只有一个全局营业状态，这与六个食堂独立运营的事实冲突。我会把持久化的食堂状态放在数据库主数据中，Redis 只作为按食堂 ID 分区的加速层。管理员修改状态时先更新数据库，再删除或更新对应缓存；菜单页可展示停业，但下单服务必须重新检查，防止页面状态过期。

- **追问：为什么不只存 Redis？** 我不会让一个核心业务状态只存在可丢失的缓存层。Redis 清空、迁移或宕机后，如果代码用默认开店回填，可能让本应停业的食堂被下单。数据库记录是真实来源，缓存只优化读路径，缓存未命中应回源而不是自行生成业务值。同时我会保留状态变更人和时间，方便定位误操作。
- **追问：营业时间和手动关店如何组合？** 我会把“是否启用”、“手动营业开关”和“当前是否在营业时段”分开建模。最终可下单状态是这些条件与异常暂停等规则的组合，不是一个含义模糊的整数。菜单页返回可读的不可下单原因，下单时用统一的领域服务再计算一次，避免页面与交易链路各自实现一套规则。

### 主问 6：管理端怎么防止查询别的食堂数据？

**第一人称口播：** 我会明确区分功能权限和数据权限。现有 RBAC 可以判断员工能否访问菜品或订单接口，但不能说明他能看哪个食堂。我会增加员工与食堂的关系，登录后从服务端解析可用 scope；普通管理员的查询条件必须使用授权食堂，不相信前端传入的 ID。超级管理员可选择食堂，但选择仍要在服务端验证。

- **追问：食堂 ID 要不要放 JWT？** 我可以把当前食堂或授权版本放进 JWT 以减少每次查询，但不会将它视为永久真实。员工调岗或被撤权后，旧 token 在过期前仍可能携带旧 scope。因此高风险写操作应检查数据库或缓存中的当前授权，或者引入授权版本使旧 token 失效。JWT 可以传递上下文，但撤权时效需要独立设计。
- **追问：在 Mapper 中自动加条件好还是显式传参好？** 我在改造初期倾向显式传入数据 scope，因为它容易审查和测试，也不会让后台任务或统计查询被隐式拦截。当数据域大量增加后，可以评估 MyBatis 拦截器，但必须对超级管理、系统任务和跨店报表定义显式的跳过规则。不管用哪种方式，测试都要包含直接篡改 ID 的越权用例。

### 主问 7：菜单缓存键如何调整？

**第一人称口播：** 现有菜单缓存主要按分类 ID 组织，而失效还使用全局版本。多食堂后，我会把食堂 ID、菜单类型、分类 ID 和版本共同放入命名空间，例如某食堂某分类的菜品列表。修改一家食堂的菜品时只增加该食堂的菜单版本，旧 key 自然过期，其他食堂的热缓存继续使用。读路径仍保留空值缓存和互斥重建边界。

- **追问：分类 ID 全局唯一，还需要食堂 ID 吗？** 从避免键冲突看，全局唯一的分类 ID 确实足够，但我仍倾向把食堂维度写入键。原因不是唯一性，而是可观测性、批量预热、局部失效和运营排查。当某家食堂出现问题时，这种命名空间能让我直接统计其热点和清理范围。但我不会把键设计当成数据隔离手段，真正隔离仍靠查询条件和服务校验。
- **追问：如何防止缓存穿透和击穿？** 我会沿用现有项目中已有的空列表缓存和互斥重建思路，但把锁粒度和缓存键一起扩展到食堂维度。不存在或已禁用的分类可缓存短 TTL 空值，热分类失效时只允许一个请求回源，其他请求在有限时间内重试或返回可接受降级。同时我会监控按食堂分组的命中率和回源次数，而不只看全局平均值。

### 主问 8：如何迁移现有单店数据？

**第一人称口播：** 我不会一次性给所有表加非空字段，因为旧数据会让迁移立即失败。我会先创建六家食堂主数据，建立一个明确的默认食堂，再以可空方式增加归属字段。按可核对规则回填分类、菜品、套餐、购物车和订单，应用在过渡期对旧数据双读。当空值数、孤儿数据和回归测试都通过后，再收紧非空、索引与外键约束。

- **追问：旧订单应回填到哪家食堂？** 我不会根据菜名猜测食堂，因为同名菜品可能存在于多店，而订单明细只保存商品快照时无法可靠还原。如果历史上真的只有一家门店，我会建立“历史默认食堂”并全量回填，保留迁移标记说明这是历史归并。如果可从旧菜品主数据稳定追溯，再分批精确回填；无证据的数据不会被伪造成精确归属。
- **追问：如何回滚？** 我会采用向前兼容的扩展式迁移，不在第一阶段删除或改名旧字段。新字段与新表先上线，旧版本应用仍能忽略它们；如果新版本出现问题，可先回滚应用并保留新数据。数据回填脚本需要冪等、分批和可核对，收紧非空约束放在所有客户端升级之后。我不会把 DDL 回退当作唯一回滚方案。

### 主问 9：小程序需要改哪些状态？

**第一人称口播：** 我会新增食堂列表页或首页的食堂选择区，用户选中后进入原有菜单页。客户端状态至少包含当前食堂 ID、名称、营业状态与可选配送配置，菜单、购物车、结算、支付和订单详情都不再硬编码“智餐食堂”。切换食堂时检查购物车，不空就弹出确认。但本地存储只是用户偏好，不能替代后端归属校验。

- **追问：返回首页是回菜单还是食堂列表？** 我会根据用户预期做分层导航。用户已有最近选择且该食堂仍可用时，可以快速回到该菜单，同时顶部提供显眼的切换入口。首次进入、食堂被停用或用户主动点击切换时，显示六家食堂列表。我不会让深层页面的返回键一律跳到食堂列表，否则会破坏正常页面栈和结算流程。
- **追问：仓库里只有已编译小程序怎么办？** 我会先寻找对应的 uni-app 或其他原始工程，因为直接维护生成后的 `vendor.js`、WXML 和页面 JS 会丢失源码映射，也容易在下次编译时被覆盖。如果确认原始工程已丢失，我会把重建可编辑小程序源码当成独立的前置任务，先对现有页面和 API 行为做回归基线，不把长期功能堆在编译产物上。

### 主问 10：如何处理秒杀套餐的食堂归属？

**第一人称口播：** 秒杀链路会直接根据套餐生成订单，它不经过普通购物车，因此不能假设普通下单的校验会自动覆盖它。我会让活动的食堂归属由对应套餐推导或显式保存，创建活动时校验套餐属于管理员有权操作的食堂。抢购和落单时再检查食堂可用、套餐未转店，并把食堂归属与快照写入订单。

- **追问：食堂关闭时已预热的秒杀怎么办？** 我会把食堂营业状态作为秒杀的外层门禁，但不把它混入单个活动的库存字段。食堂关闭时，读接口可将活动标记为不可抢购，写接口在执行 Lua 或扣减库存前检查带版本的营业状态。已完成预留但未落单的请求要有明确回滚路径，避免关店导致库存长期占用或订单落到错误的履约主体。
- **追问：秒杀缓存键需要带食堂 ID 吗？** 如果活动 ID 全局唯一，技术上不带食堂 ID 也不会冲突，但我仍会评估在命名空间中加入食堂维度，方便限流、监控和局部下线。更重要的是，Lua 脚本或原子扣减的输入必须由服务端根据活动和套餐反查生成，不能让用户自由组合食堂 ID 和活动 ID。键名只是组织手段，归属校验才是安全边界。

### 主问 11：红包和配送费怎么处理？

**第一人称口播：** 这里我不会默认六家食堂的规则完全相同。现有代码把配送费作为固定常量，红包也更接近全局资产。我会先与业务确认这些是平台级、食堂级还是两者兼容。配送费、起送价和可配送范围建议作为食堂配置，结算时由服务端重算。红包则增加适用范围，使用时同时验证用户归属、有效期、门槛和食堂范围。

- **追问：全平台红包如何表示？** 我会避免在每张红包上复制六条食堂关系。一种做法是红包模板有适用范围类型，如全平台或指定食堂；只有指定范围时才维护中间表。用户领取的资产保留对模板或规则快照的引用，使用时在服务端校验当前订单食堂。如果业务要求领券后规则不能改变，则需在用户资产上保存必要规则快照。
- **追问：为什么不接受小程序计算的最终金额？** 因为小程序运行在用户端，请求中的菜品价格、配送费、食堂 ID 和红包折扣都可被篡改。我会让客户端传选择结果，例如红包资产 ID，后端从当前购物车和食堂配置读取真实数据，重新计算菜品金额、打包费、配送费和优惠。如果前后端展示不一致，返回新报价让用户确认，不用客户端金额落单。

### 主问 12：旧校园餐饮迁移为什么说不完整？

**第一人称口播：** 我从仓库证据看到迁移已经建了校区、食堂、档口、取餐点和餐段，也给菜品与订单加了部分字段。但 Java 实体中没有这些归属，订单插入 SQL 也没有写入食堂字段；分类仍然是全局，套餐未与食堂或档口关联，购物车也没有食堂归属。所以这只是数据库草案和样例数据，不是可用的多食堂业务链路。

- **追问：这份迁移能直接修改吗？** 如果 Flyway 已经在任何数据库执行过，我不会直接修改已发布迁移，因为校验和将失配，也无法保证各环境的演进一致。我会先查看迁移历史和实际表结构，再新建后续版本来补齐表、字段、回填和约束。只有确认该迁移从未在任何共享环境执行、且团队同意重建基线时，才考虑重写原文件。
- **追问：其中的档口模型要保留吗？** 我会根据真实履约需求决定，而不是因为表已存在就强行使用。如果用户只选六个食堂，由食堂统一接单、收款和配送，档口可以作为可选的展示分组，不进入订单核心模型。如果档口独立接单、库存和出餐，就要重新评估跨档口购物车、子单、取餐码和对账，这是不同级别的项目。

### 主问 13：多食堂架构如何测试？

**第一人称口播：** 我会把“不串店”当成可执行的不变式，而不是只测页面是否显示六张卡片。测试数据至少包含两家食堂的同名分类、同名菜品和不同营业状态。接口测试覆盖菜单过滤、跨店加车、篡改 ID 下单、关店后下单、管理员越权和缓存局部失效。还要做并发加车与下单测试，确保业务校验不会被时序绕过。

- **追问：最关键的三个用例是什么？** 第一个是用户先向一食堂加菜，再向二食堂加菜，服务端必须拒绝或要求明确切店，最终购物车不能混店。第二个是构造食堂 A 的请求却携带食堂 B 的菜品 ID，下单必须在扣库存前失败。第三个是食堂 A 管理员传入食堂 B 的订单 ID，即使他拥有订单接口权限，也必须被数据 scope 拒绝。
- **追问：如何验证缓存没串店？** 我会为两家食堂准备相同分类名但不同 ID 与不同商品列表，分别预热后重复读取，验证结果和缓存键都带有正确归属。然后修改其中一家食堂的菜品，检查它的旧版本不再被读取，而另一家的缓存仍命中。还要传入不匹配的食堂和分类组合，确保 loader 的数据库查询本身也带双重条件，而不是只信任缓存键。

### 主问 14：为什么当前不建议上微服务？

**第一人称口播：** 六个食堂是业务维度增加，不等于请求规模、团队组织或发布节奏已经需要微服务。当前最大的风险是数据归属没有贯通，如果过早拆分，购物车、库存和订单的一致性还会变成分布式事务问题。我会先在模块化单体中划清食堂主数据、菜单、交易和权限边界，通过包结构和服务接口控制依赖，有独立扩缩或团队边界证据后再拆分。

- **追问：什么信号出现时可以拆？** 我会关注可量化的技术和组织信号，例如菜单读流量与交易写流量的扩缩策略完全不同，秒杀对资源的冲击经常影响普通订单，不同团队需要独立发布，或者单体的故障域无法通过限流和资源隔离控制。即使决定拆分，我也会先确定数据所有权和事件契约，避免多个服务共享同一组表而只是形式上拆分。
- **追问：单体怎么保持可拆分性？** 我会让不同业务模块通过明确的应用服务交互，避免菜单模块直接修改订单表，也避免控制器跨过服务层组装多个 Mapper。关键的业务事件，例如菜品状态变更和订单完成，先在进程内使用稳定语义的事件。包级可见性、依赖方向测试和模块自有表清单可帮助约束边界，将来真的拆服务时才不会从一团隐式调用开始。

### 主问 15：你会如何分阶段交付？

**第一人称口播：** 我会把交付分成可验证的纵向切片。第一阶段确定业务规则并完成数据库扩展与旧数据回填；第二阶段打通用户侧食堂列表、按店菜单、同店购物车和普通下单，这是最小可用闭环；第三阶段补管理端食堂 scope、分店订单和营业状态；第四阶段改造秒杀、红包、统计与缓存预热。每阶段都以不串店回归测试作为准入条件。

- **追问：最小可用版本可以暂时不做什么？** 在业务确认六个食堂统一运营、暂时不需要档口独立接单的前提下，最小版本可以不做校区多层级、档口子单、独立结算和复杂餐段排班。也可以先让红包保持全平台适用，但这必须是明确的业务规则而不是遗漏。不能暂缓的是购物车同店约束、订单食堂归属、服务端校验和管理端基本数据隔离。
- **追问：你如何判断交付完成？** 我不会以“六个食堂能在页面上点开”作为完成标准。最小闭环要求每家食堂能独立上下架菜单、开关营业，用户只能在一家店结算，订单正确记录食堂归属与快照，对应管理员只能查看和处理授权订单。还要通过篡改 ID、并发加车、停业瞬间下单、缓存失效和旧数据回归用例，性能收益则单独建立基线后再评估。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 校园数据库草案 | `sky-server/src/main/resources/db/migration/V20260909__campus_dining_schema.sql` | 主问 1、12 |
| 分类全局查询 | `CategoryServiceImpl.list`、`CategoryMapper.xml#list` | 主问 1、3 |
| 购物车仅按用户查询 | `ShoppingCartServiceImpl`、`ShoppingCartMapper.xml#list` | 主问 2 |
| 下单从用户全量购物车落单 | `OrderServiceImpl.submitOrder`、`createOrderFromMessage` | 主问 2、4 |
| 订单 SQL 未写入食堂字段 | `sky-server/src/main/resources/mapper/OrderMapper.xml#insert` | 主问 4、12 |
| 全局营业状态 | `controller/admin/ShopController.java`、`controller/user/ShopController.java` | 主问 5 |
| 管理端仅有功能 RBAC | `JwtTokenAdminInterceptor.java`、`PermissionMapper.countEmployeePermission` | 主问 6 |
| 按分类的菜单缓存 | `MenuCache.java`、`MenuCacheWarmup.java` | 主问 7 |
| 秒杀直接生成订单 | `SeckillServiceImpl.java` | 主问 10 |
| 已编译小程序 | `../mp-weixin/app.json`、`../mp-weixin/pages/index/index.wxml`、`../mp-weixin/common/vendor.js` | 主问 9 |

## 交给 /asu 的项目事实摘要

- 项目：智餐外卖多食堂改造；目标岗位待补。
- 个人责任边界：当前只能确认用户提出了设计需求，未确认实现和上线 ownership。
- 关键技术动作：计划建立食堂聚合边界、同店购物车、订单快照、分区缓存与数据 scope。
- 可核验证据：仓库存在旧校园数据库草案，现有 Java 模型、购物车、订单、营业状态仍是单店实现。
- 候选简历表述：只有在功能完成且通过回归测试后，才使用本文 bullet。
- 待补：实际个人责任、上线环境、用户量、数据量、延迟、缓存命中率和故障证据。

## 交给 /interview 的高风险 Claim 清单

- Ownership Claim：“主导”、“负责”、“从 0 到 1”均无证据，当前不能使用。
- Metric Claim：无已测性能、用户数、效率或错误率数据。
- Architecture Claim：多食堂聚合、数据 scope、同店购物车和分区缓存尚属设计，未有实现证据。
- Result Claim：不能声称已上线、已支持六食堂或已提升性能。
